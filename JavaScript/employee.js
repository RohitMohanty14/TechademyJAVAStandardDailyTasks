function generateEmployeeProfile(name, department, jobTitle = "Associate") {
  const profile = `Employee Profile: 
  Name: ${name} 
  Department: ${department} 
  Job Title: ${jobTitle}`;
  console.log(profile);
}

